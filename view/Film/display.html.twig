<!DOCTYPE html>
<html>
<head>
   <meta charset="utf-8"/>
   <title>{{ film.titre }}</title>
   <base href="http://localhost/annuaire_films/"/>
</head>
<body>
   <header>
      <h1>{{film.titre}}</h1>
   </header>
   <p>{{ film.synopsis }}</p>
   <img src="{{ film.images }}" alt="{{ film.titre }}">
   <p>{{ film.annee }}</p>
   
   <footer>
      <p>Pied de page...</p>
   </footer>
</body>
</html>
